import redis
import time

Med_reg = dict()
Re_Button_Pressed = 0

def main():
    if Med_reg['data'] == 'good':
        #send information interface, notify the user
        if Re_Button_Pressed == 1:
            #send

